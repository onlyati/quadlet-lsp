package semantic

// SecretValueTokens parses values like 'gitea-db-password,type=env,target=GITEA__database__PASSWD'
func SecretValueTokens(value string) []token {
	var tokens []token

	return tokens
}
